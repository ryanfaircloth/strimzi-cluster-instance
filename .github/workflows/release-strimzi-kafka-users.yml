name: Release strimzi-kafka-users

on:
  push:
    tags:
      - "strimzi-kafka-users/v*"

env:
  REGISTRY: ghcr.io

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: "3.14.0"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version from tag
        id: version
        run: |
          VERSION="${GITHUB_REF_NAME#*/v}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Package Helm chart
        run: |
          mkdir -p .out
          helm package "./strimzi-kafka-users" \
            --version "${{ steps.version.outputs.version }}" \
            --destination .out

      - name: Push Helm chart to GHCR
        run: |
          CHART_PACKAGE=".out/strimzi-kafka-users-${{ steps.version.outputs.version }}.tgz"
          CHART_REPO="oci://${{ env.REGISTRY }}/${{ github.repository_owner }}/strimzi-kafka-users"
          echo "Pushing ${CHART_PACKAGE} to ${CHART_REPO}"
          helm push "${CHART_PACKAGE}" "${CHART_REPO}"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            --title "strimzi-kafka-users v${{ steps.version.outputs.version }}" \
            --notes "Release of strimzi-kafka-users v${{ steps.version.outputs.version }}" \
            --target "${{ github.sha }}"
